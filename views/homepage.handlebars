<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Homepage</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <img class="h-auto max-w-sm rounded-sm" src="/images/spider.png">
<div class="container mx-auto p-8">
  <h1 class="text-4xl font-bold text-center mb-8">Welcome to Bite Buddy</h1>

  {{!-- Loop over each object in the bites array --}}
  {{#each bites as |bite| }}
  <div class="bg-white rounded-lg shadow-lg mb-8">
    <div class="p-8">
      <h2 class="text-2xl font-bold mb-4">
        <a href="/bites/{{bite.id}}" class="text-indigo-500 hover:underline">{{bite.headline}}</a>
      </h2>
      <p class="text-gray-500 mb-4">Created by {{bite.user.name}} on {{format_date bite.date_created}}</p>
      <p class="text-gray-800">{{bite.content}}</p>
    </div>
  </div>
  {{/each}}
</div>

</body>
</html>
